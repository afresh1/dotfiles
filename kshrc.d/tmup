tmup() {
    eval $(
      local IFS="="
      tmux showenv | while read key value ; do
        if [ ${key} != ${key#-} ]; then
          echo unset ${key#-}
        else
          echo $key="'$value'"
        fi
      done
    )
}
